apiVersion: v1
kind: Service
metadata:
  name: network-multitool
  labels:
    app: network-multitool
spec:
  type: ClusterIP
  selector:
    app: network-multitool
  ports:
    - name: http
      port: 80
      targetPort: http
    - name: https
      port: 443
      targetPort: https
